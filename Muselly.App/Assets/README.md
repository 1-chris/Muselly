# Assets

Bundled application assets (icons, images, fonts) live here. Everything under this
folder is embedded via the `AvaloniaResource Include="Assets\**"` glob in
`Muselly.App.csproj` and is addressable as `avares://Muselly.App/Assets/...`.
